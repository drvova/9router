// Shared geometry for the header's icon-only action buttons.
// Matches the `ghost` variant and `md` height of Button.js so the whole
// header row lines up on one 36px baseline instead of four different ones.
export const HEADER_ACTION_CLASS =
  "flex items-center justify-center size-9 shrink-0 rounded-[10px] text-text-muted hover:bg-surface-2 hover:text-text-main transition-colors";
